{
  "name": "project",
  "version": "1.0.0",
  "description": "Search all files with string that gets as input the string that gets as input. ",
  "main": "search.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lidor Abo",
  "license": "ISC"
}
